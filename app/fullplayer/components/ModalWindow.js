class ModalWindow {
    constructor() {
        let parentContainer = null;

        this.setup();
    }

    setup() {
        // throw error if cant find container elem
    }

    // Private members =========================================================



    // Public members ==========================================================
    
    open() {
        /*  */
    }
};
